{#-
 # Inline notice shown next to a wizard field, styled by the plugin's own
 # `alertelt` CSS rather than by a Bootstrap alert.
 #
 # Expected context:
 #  - message: plain-text notice
-#}
<div class="alertelt active"><div class="alertelttext"><span>{{ message }}</span></div></div>
