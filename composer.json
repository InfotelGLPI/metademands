{
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "autoloader-suffix": "GlpiPluginMetademands",
        "platform": {
            "ext-gd": "8.3"
        }
    },
    "require-dev": {
        "glpi-project/tools": "^0.8"
    },
    "require": {
        "setasign/fpdf": "^1.9"
    }
}
